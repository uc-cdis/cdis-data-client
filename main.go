package main

import "github.com/uc-cdis/cdis-data-client/cmd"

func main() {
	cmd.Execute()
	// test := jwt.Functions{Config: new(jwt.Configure), Request: new(jwt.Request)}
	// test.DoRequestWithSignedHeader(test.Requesting, "default", "text")
}
